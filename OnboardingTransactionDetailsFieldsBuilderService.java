GET /cases/{caseBusinessIdentifier}/tasks/change-client-information
* The API returns the current information retrieved from CLASS.
* The API returns the current user input already stored for the task.
* The response uses the internal CCI model.
* If no user input exists, the editable section is initialized with the values coming from the Digital Fact Find.
